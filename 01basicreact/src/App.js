import Chai from "./Chai";

function App() {
  return (
   <>
   <Chai />
   </>
  );
}

export default App;

// best practices
// the function we are importing is components only 
// In react or in vite , the function name should start from CAPITAL LETTERS like (import Chai)
// some library (like vite) will say that if you are returning html file then the name must be .jsx


